language: python
python:
    - "3.6"

before_install: cd sphinx

sudo: false
cache: pip

install: "pip install sphinx"
install: "pip install sphinx_rtd_theme"
script: sphinx-build -nWT -b html -d _build/doctrees  . _build/html